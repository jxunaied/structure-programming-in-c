#include<stdio.h>
#include<conio.h>
    void main()
    {
        char ar[50],br[50],a,b;
        int i,j,n,k;

        printf("\n Please Give The STRING OF A : ");
        scanf("%s",ar);
        printf("\n Please Give The STRING OF B : ");
        scanf("%s",br);

        for(i=0;br[i]!='\0';i++){
            if(a==0){
                b=a;
            }

            ar[i]=br[i];}
        ar[i]='\0';
        printf("\n AFTER COPYING STRING B INTO A IS  %s ",ar);
        getch();
    }
